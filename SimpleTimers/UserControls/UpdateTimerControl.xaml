<UserControl
    x:Class="SimpleTimers.UserControls.UpdateTimerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleTimers.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="160"
    d:DesignWidth="300">

	<StackPanel>
		<TextBlock FontSize="60" HorizontalAlignment="Center" x:Name="CountdownTimer" Text="00:10:00"/>

		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
			<ComboBox x:Name="SelectedHour" SelectedIndex="0" Header="Hours" ItemsSource="{x:Bind Hours}"/>
			<ComboBox x:Name="SelectedMinute" SelectedIndex="10" Header="Minutes" ItemsSource="{x:Bind Minutes}"/>
			<ComboBox x:Name="SelectedSecond" SelectedIndex="0" Header="Seconds" ItemsSource="{x:Bind Seconds}"/>
			<Button Margin="5 0 0 0" VerticalAlignment="Bottom" x:Name="UpdateTimerButton" Content="Update" Click="UpdateTimerButton_Click"/>
		</StackPanel>

	</StackPanel>
</UserControl>
