<UserControl
    x:Class="SimpleTimers.UserControls.UseTimerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleTimers.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="160"
    d:DesignWidth="300">

    <StackPanel>
		<TextBlock HorizontalAlignment="Center" Text="{x:Bind Timer.Name, FallbackValue='Test'}"/>
		<TextBlock FontSize="60" HorizontalAlignment="Center" x:Name="CountdownTimer" Text=""/>
		<StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
			<Button Background="DarkTurquoise"  x:Name="Start" Click="Start_Click">
				<SymbolIcon Symbol="Play"/>
			</Button>
			<Button x:Name="Stop" Background="Goldenrod" Margin="5" Click="Stop_Click">
				<SymbolIcon Symbol="Pause"/>
			</Button>
			<Button x:Name="Reset" Background="DarkRed" Click="Reset_Click">
				<SymbolIcon Symbol="Refresh"/>
			</Button>
		</StackPanel>
	</StackPanel>
</UserControl>
